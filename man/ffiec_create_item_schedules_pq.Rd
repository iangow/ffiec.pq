% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ffiec_create_item_schedules_pq.R
\name{ffiec_create_item_schedules_pq}
\alias{ffiec_create_item_schedules_pq}
\title{Create FFIEC item-schedule coverage Parquet file}
\usage{
ffiec_create_item_schedules_pq(
  out_dir = NULL,
  schema = "ffiec",
  schedules = NULL,
  overwrite = FALSE,
  file_name = "ffiec_item_schedules.parquet",
  progress = TRUE,
  cache = TRUE,
  cache_file = NULL
)
}
\arguments{
\item{out_dir}{Optional output directory. If \code{NULL}, the directory is
resolved using \code{schema} and environment variables.}

\item{schema}{Schema name passed to \code{resolve_out_dir()}.}

\item{schedules}{Optional character vector of schedules to include
(e.g., \code{c("rc", "rcn")}). If \code{NULL}, all schedules are included
except \code{"por"} and \code{"items"}.}

\item{overwrite}{Logical; whether to overwrite an existing output file.}

\item{file_name}{Output parquet file name.}

\item{progress}{Logical; whether to show a progress bar while reading schemas.}

\item{cache}{Logical; whether to cache file schemas in an RDS file for
faster repeated runs.}

\item{cache_file}{Optional cache file path. If \code{NULL}, defaults to
\code{file.path(out_dir, ".ffiec_item_schedules_schema_cache.rds")}.}
}
\value{
A tibble with one row describing the file written.
}
\description{
Scans FFIEC schedule Parquet files in the resolved output directory and
creates a Parquet file describing which items (columns) appear in which
schedules and on which reporting dates. Dates are inferred from file names.
}
\details{
Includes optional filtering by schedule, a progress indicator, and a local
cache (RDS) of Parquet schemas to speed up repeated runs.
}
