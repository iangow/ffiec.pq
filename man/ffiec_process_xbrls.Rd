% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ffiec_process_xbrls.R
\name{ffiec_process_xbrls}
\alias{ffiec_process_xbrls}
\title{Process FFIEC XBRL bulk zip files}
\usage{
ffiec_process_xbrls(
  zipfiles = NULL,
  raw_data_dir = NULL,
  data_dir = NULL,
  schema = "ffiec",
  use_multicore = FALSE,
  ns_prefix = "cc",
  prefix = ""
)
}
\arguments{
\item{zipfiles}{Optional character vector of FFIEC bulk XBRL zip file paths.
If \code{NULL}, zip files are discovered automatically in the resolved input
directory (using \code{ffiec_list_zips(type = "xbrl")}).}

\item{raw_data_dir}{Optional parent directory containing FFIEC bulk zip files.}

\item{data_dir}{Optional parent directory for Parquet output.}

\item{schema}{Schema subdirectory name (default \code{"ffiec"}). Set to
\code{NULL} to use \code{raw_data_dir} / \code{data_dir} directly.}

\item{use_multicore}{Logical; if \code{TRUE} and packages \code{future} and
\code{furrr} are installed, process zip files in parallel using
\code{future::multisession()}.}

\item{ns_prefix}{XML namespace prefix to extract (default \code{"cc"}).}

\item{prefix}{Optional filename prefix for output Parquet file names.}
}
\value{
A tibble with one row per processed zipfile, including the output
  Parquet basename. The attribute \code{"out_dir"} contains the resolved
  output directory.
}
\description{
Reads FFIEC bulk XBRL zip files (each containing many \code{*.xbrl.xml} inner
files), extracts reported XBRL facts, and writes a Parquet file per zipfile
into the resolved Parquet output directory.
}
\details{
Directory resolution follows the same rules as \code{ffiec_process()}:
\code{raw_data_dir} / \code{RAW_DATA_DIR} for inputs and
\code{data_dir} / \code{DATA_DIR} for outputs, optionally with a schema
subdirectory when \code{schema} is non-\code{NULL}.
}
